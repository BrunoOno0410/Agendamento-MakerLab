import { styled } from '../../styles/theme';

export const MainDiv = styled('div', {
    height: '88%',
    width: '100%',
    backgroundColor: '$white', //color test
    display: 'flex',
    alignItems: 'center',
    
});

